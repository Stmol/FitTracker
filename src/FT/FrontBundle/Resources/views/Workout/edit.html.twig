{% extends '::base.html.twig' %}

{% block body -%}
  {{ form(edit_form, { 'attr': { 'novalidate': 'novalidate' } }) }}

  <a href="{{ path('workouts') }}">{{ 'workout.list'|trans }}</a>
{% endblock %}
